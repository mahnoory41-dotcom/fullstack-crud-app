function Navbar() {
  return (
    <nav
      style={{
        background: "#2563eb",
        color: "white",
        padding: "15px",
        fontSize: "22px",
        fontWeight: "bold",
      }}
    >
      Laravel + React CRUD
    </nav>
  );
}

export default Navbar;